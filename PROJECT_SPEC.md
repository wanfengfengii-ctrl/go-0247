# TorqueChain 高强螺栓连接检验与交工签收闭环服务

## 项目目标

TorqueChain 面向钢结构工程质量实验室，围绕一次构件节点高强螺栓连接检验任务，冻结节点、螺栓序列、连接副批次、抽检集合、设计预紧力、允许扭矩区间、设备校准版本和施工代次，依次完成配套核验、扭矩系数复验、按序初拧与终拧、抽检复核、双人验收，并产生唯一交工签收、返工隔离或取消终局。Go 后端采用 SQLite WAL、事务写入、追加式审计和启动恢复；脚本化扭矩设备适配器支持可控拒绝、断连和重试。仓库包含 go.mod、Go HTTP/JSON API、真实质检操作页面、多阶段双架构 Dockerfile，可构建 linux/amd64 与 linux/arm64。生产实现目标为 22-26 个 Go 文件、约 2500 行有效代码、至少 6 个有职责的包；公共测试至少 4 个文件、16 个确定性用例。

## 端到端业务流程

1. 实验室从构件节点目录、螺栓/螺母/垫圈批次目录、连接副配套规则、套筒规格和校准设备目录创建任务；锁定操作生成不可变工程节点摘要、节点内螺栓顺序、抽检集合、设计预紧力、允许扭矩区间、设备校准版本和施工代次。
2. 任务进入配套核验中后，操作员逐个提交连接副身份与套筒信息；系统比对锁定批次、材料等级、规格和修订，全部通过后进入扭矩复验中。
3. 扭矩复验通过脚本化设备完成规定试验组，记录扭矩系数、设备回执和校准版本；越界、设备拒绝或断连形成明确待重试记录，成功后进入安装终拧中。
4. 每个节点维护初拧前缀和终拧前缀；初拧必须连续推进，终拧只能在同一螺栓初拧完成后按锁定顺序连续推进，记录整数牛米扭矩、角度、时间窗口、设备租约和操作号。
5. 终拧全部完成后进入抽检复核中；预定抽检集合中的每个螺栓必须提交不可覆盖的复检结果链，结果不足、超限、缺失或相互矛盾会将相关节点置于返工隔离。
6. 两名不同且具备资格的复核人员独立确认同一任务摘要和证据链；全部节点合格且抽检闭合后任务进入可签收。签收、返工隔离和取消通过终局仲裁器竞争，只产生一个不可变凭据。

## 核心组件与职责

1. 钢构件与连接副规则目录：维护工程节点、节点螺栓序列、螺栓/螺母/垫圈批次与材料等级、套筒规格、抽检规则、预紧力/扭矩区间、设备校准版本及资格人员目录，所有修订可追溯。
2. 螺栓检验任务聚合：实现任务代次、锁定快照、状态机、节点前缀游标、当前修订和终局栅栏，拒绝陈旧修订及终态写入。
3. 连接副与设备占用账簿：为每个连接副签发一次性占用令牌，为扭矩设备维护带校准版本和截止时间的租约；并发领用、换装、启动和释放在同一事务中原子裁定。
4. 初拧终拧及抽检记录账簿：追加不可覆盖的配套核验、扭矩复验、初拧、终拧、设备回执和抽检结果链，保存操作号幂等内容摘要。
5. 复核与终局仲裁器：计算按节点与螺栓号排序的原因集合，执行双人独立复核、交工签收、返工隔离和取消的唯一终局竞争并生成凭据。
6. Go HTTP API 及真实质检操作页面：提供目录查询、任务锁定、批次核验、设备领用、拧紧登记、抽检复核、签收/隔离/取消和审计查询；页面仅承载这些现场操作。

## 领域规则与不变量

1. 任务锁定时必须固定工程节点摘要、节点编号及顺序化螺栓清单；螺栓号在节点内唯一，顺序为从 1 开始的连续整数，锁定后任何目录修订不能改变快照。
2. 连接副由螺栓、螺母、垫圈批次和材料等级组成；配套规则还绑定套筒规格。未知、重复、错配、已消耗或陈旧修订的身份核验不得推进任何节点前缀。
3. 每个连接副令牌在开放任务中最多有一个有效占用，令牌一旦绑定并消费不可再次绑定其他螺栓；取消前仅可释放未消费令牌。每台扭矩设备在所有开放任务中最多一份有效租约，租约包含校准版本、持有人、修订和过期时间。
4. 任务状态严格使用待锁定、配套核验中、扭矩复验中、安装终拧中、抽检复核中、可签收、已签收、返工隔离、已取消；只有规定的相邻转换允许发生，返工隔离和已取消不可恢复。
5. 每个节点拥有初拧游标与终拧游标。初拧只能提交当前游标螺栓并成功后加一；终拧必须在该螺栓初拧完成、前一终拧已完成且任务仍处于安装终拧中时提交，任何跳号保持游标不变。
6. 扭矩读数使用整数牛米，角度使用整数度，必须落在锁定的允许区间和设备规定范围内；时间戳必须在任务施工窗口内且来自可注入单调时钟。越界、设备拒绝、断连或租约过期只生成待重试证据，不产生有效拧紧记录。
7. 扭矩系数复验必须满足固定试验数量、系数区间、设备校准版本和连续时间窗口；失败会阻止进入安装终拧中，重试使用新的记录序号。
8. 相同操作号及完全相同规范化内容重试返回原结果且不重复写入；相同操作号内容不同返回 IDEMPOTENCY_CONFLICT。
9. 抽检集合在锁定时固定且按节点、螺栓号排序；每个抽检螺栓必须形成一次有效复检结论。低于下限、高于上限、缺失、重复有效结论或矛盾链均使相关节点进入返工隔离，原始记录不可覆盖。
10. 复核人员必须具备当前资格版本且两人身份不同；同一人员不能代表两个独立复核席位。签收要求所有节点终拧合格、抽检闭合、无活动租约异常并有两份独立复核摘要。
11. 签收、返工隔离和取消在任务级以条件事务竞争；胜者写入唯一凭据和终态，失败者得到稳定 FINALIZED_CONFLICT 或 TERMINAL_STATE 错误。终态后的迟到拧紧、设备回执、抽检、普通操作和租约变更一律拒绝且不改变状态。
12. 每次拒绝都返回稳定错误码、当前修订和按节点编号再按螺栓号排序的可复现原因；事务失败不得留下部分令牌、设备租约、拧紧记录、抽检结果或状态修订。

## 数据模型与持久化

1. ComponentNode：工程、构件节点编号、几何/连接摘要、节点修订和有序 BoltSpec 列表。
2. BoltBatch、NutBatch、WasherBatch：批次号、材料等级、制造商、检验状态和目录修订；CompatibilityRule：允许的三件套组合及套筒规格。
3. TorqueDevice：设备编号、支持套筒、校准证书版本、扭矩范围、状态；DeviceLease：任务代次、持有人、校准版本、租约修订、过期时间和释放状态。
4. InspectionTask：任务编号、施工代次、锁定快照摘要、状态、聚合修订、节点集合、抽检集合、设计预紧力、扭矩区间和终局凭据。
5. ConnectionToken：连接副令牌、绑定节点/螺栓、批次摘要、消费状态、占用者和唯一性约束。
6. PairVerification、TorqueCoefficientRun：操作号、输入摘要、设备回执、系数/窗口数据、结果和重试序号。
7. TighteningRecord：阶段初拧或终拧、节点、螺栓号、扭矩牛米、角度、单调时间、设备租约、前缀前后值、操作号及不可变摘要。
8. SamplingResult：抽检螺栓、尝试序号、测量预紧力、允许区间、设备/人员、结果枚举、证据摘要和链前哈希；有效结论只能登记一次。
9. Review：席位、人员资格版本、任务摘要、证据摘要、独立签名时间；FinalDecision：终局类型、胜者操作号、原因摘要、凭据和提交时间。
10. IdempotencyEntry、AuditEvent、SchemaVersion：规范化请求摘要、原始结果、事件序号、事务提交标记和 SQLite WAL 恢复元数据。

## 公开接口

1. GET /v1/catalog/nodes、/batches、/devices、/personnel 返回固定目录及修订；POST /v1/tasks 接收节点、批次、抽检集合、设备和施工代次，执行锁定。
2. POST /v1/tasks/{id}/pair-verifications、/torque-rechecks、/tightening/initial、/tightening/final、/sampling-results 分别提交身份核验、扭矩系数复验、初拧、终拧和抽检记录；请求必须带 task_revision、operator_id、operation_no 和规范化 payload。
3. POST /v1/tasks/{id}/leases/claim、/leases/release 原子申请或释放连接副令牌及设备租约；设备适配器接口提供 Start、Read、Stop，支持脚本化故障与回执序列。
4. POST /v1/tasks/{id}/reviews 提交第一或第二独立复核；POST /v1/tasks/{id}/finalize/sign、/finalize/quarantine、/finalize/cancel 竞争终局并返回唯一凭据或稳定冲突码。
5. GET /v1/tasks/{id} 返回状态、修订、节点前缀、活动租约、抽检闭合度、排序后的原因和审计摘要；所有错误使用统一 JSON envelope，限制请求体 64 KiB、字段数量、字符串长度并拒绝未知字段。
6. 真实操作页面提供任务锁定表单、批次核验表、设备领用与租约状态、初拧/终拧连续录入、抽检结果链、双人复核和终局按钮；页面通过同一 HTTP API 工作，不承担业务裁定。
7. 内部包建议为 internal/catalog、internal/task、internal/ledger、internal/records、internal/arbiter、internal/store、internal/api 及 cmd/torquechain；store 为技术支撑包，核心领域组件仍限制为六项。

## 失败边界

1. 节点、批次、连接副或设备修订不匹配返回 NODE_MISMATCH、BATCH_MISMATCH、PAIR_MISMATCH、STALE_REVISION；原因按节点和螺栓号稳定排序。
2. 并发令牌或设备申请使用唯一索引和条件更新，失败返回 TOKEN_BUSY 或 DEVICE_BUSY；事务回滚时不残留任一占用。
3. 套筒规格、材料等级、连接副重复或未知返回 SOCKET_MISMATCH、GRADE_MISMATCH、DUPLICATE_PAIR、UNKNOWN_PAIR，且不移动配套核验游标。
4. 顺序跳跃、阶段不符、节点不存在或施工代次错误返回 SEQUENCE_GAP、INVALID_PHASE、NODE_NOT_FOUND、GENERATION_MISMATCH。
5. 扭矩/角度/时间越界返回 TORQUE_OUT_OF_RANGE、ANGLE_OUT_OF_RANGE、TIME_WINDOW_VIOLATION；设备拒绝、断连、校准过期返回 DEVICE_REJECTED、DEVICE_DISCONNECTED、CALIBRATION_EXPIRED，并仅写入待重试证据。
6. 操作号内容冲突返回 IDEMPOTENCY_CONFLICT；同内容重试读取已提交结果。SQLite 提交前任何中断点都不得发布内存变更。
7. 抽检重复有效结果、结果矛盾、缺失或不在区间返回 SAMPLE_CONFLICT、SAMPLE_MISSING、SAMPLE_OUT_OF_RANGE，并原子转入返工隔离；原始链记录保留。
8. 复核人员不合格或重合返回 REVIEWER_NOT_QUALIFIED、REVIEWER_OVERLAP；签收竞争失败返回 FINALIZED_CONFLICT，终态后所有写入返回 TERMINAL_STATE。
9. 设备租约到期、任务取消或进程重启时，恢复器按提交标记重放完整记录，先回收过期未消费租约，再恢复任务游标和唯一终局；损坏日志尾部被隔离且不影响已提交事务。

## 验收标准

1. 锁定时固定工程节点摘要、螺栓编号与顺序、螺栓/螺母/垫圈批次及等级、抽检集合、设计预紧力、允许扭矩区间、设备校准版本和施工代次。
2. 每个连接副令牌、每台扭矩设备在开放任务中只能持有一个有效占用，并发领用、换装或启动必须原子裁定。
3. 配套核验必须匹配锁定批次、等级和套筒规格，未知、重复、错配或陈旧修订不得改变任何节点前缀。
4. 初拧与终拧只能按每个节点锁定顺序连续推进；整数牛米读数、角度和时间窗口越界或设备故障只能形成明确待重试记录。
5. 相同操作号同内容重试幂等而内容冲突报错；失败事务不产生半条拧紧记录或设备租约。
6. 抽检螺栓必须各有一次不可覆盖的有效复检；预紧力不足、超限、缺失或矛盾结果只能将相关节点置于返工隔离。
7. 全部节点终拧合格、抽检闭合，并由两名不同合格人员独立复核后，竞争成功者才可生成唯一交工签收凭据。
8. 并发签收、返工隔离与取消只能形成一个终局，失败竞争者得到确定错误；终态后的迟到拧紧、设备回执、抽检或普通操作一律被拒绝且不改变状态。

## 确定性测试场景

1. 锁定固定的两个虚构节点、连续螺栓序列、批次等级、抽检集合和设备校准版本，验证目录修订后任务快照不变。
2. 提交节点或批次错配、套筒规格不符、材料等级不符、未知连接副和重复令牌，断言稳定错误码、排序后的原因及零前缀变化。
3. 使用同步屏障让两个任务并发申请同一连接副令牌，断言仅一方成功、另一方 TOKEN_BUSY，数据库无双重占用。
4. 使用同步屏障让两个任务并发领用同一扭矩设备，断言单一有效租约和确定失败结果。
5. 验证旧 task_revision、旧施工代次和旧目录修订在锁定后被拒绝。
6. 尝试跳过节点内螺栓号、先终拧后初拧或跨阶段提交，断言 SEQUENCE_GAP/INVALID_PHASE 且游标不变。
7. 提交整数牛米、角度和时间窗口边界值及越界值，验证边界接受、越界拒绝和待重试记录。
8. 脚本化设备返回拒绝、断连、校准过期后重试，验证没有半条拧紧记录或租约，恢复后可用新操作号成功。
9. 验证相同操作号完全相同内容返回原结果并保持单条记录；修改一个字段返回 IDEMPOTENCY_CONFLICT。
10. 在抽检链中提交不足、超限、缺失、重复有效和矛盾结果，验证不可覆盖链、稳定原因排序和返工隔离。
11. 让同一合格人员提交两个复核席位、让不合格人员复核，验证 REVIEWER_OVERLAP/REVIEWER_NOT_QUALIFIED。
12. 用两个独立复核人员提交相同摘要，验证任务进入可签收；摘要不一致时保持抽检复核中并返回原因。
13. 通过并发屏障竞争签收、返工隔离和取消，验证只有一个终局凭据，其余请求获得确定冲突码。
14. 在已签收、返工隔离和已取消状态提交迟到拧紧、设备回执、抽检和普通操作，验证 TERMINAL_STATE 且状态、账簿和审计不变。
15. 在 SQLite 提交中断点和进程重启后恢复，验证已提交任务、租约、前缀、抽检链和终局完整，未提交对象全部不存在。
16. 使用假单调时钟、固定设备回执和固定事件序列重复运行 API 与领域测试，验证结果、错误原因排序和审计摘要字节级确定。

## 组件追踪关系

1. 目录修订、节点摘要与配套规则由 catalog 提供，覆盖锁定快照、批次错配、材料等级和套筒规格测试。
2. 状态转换、代次、节点初拧/终拧前缀和陈旧修订由 task 聚合负责，覆盖顺序跳跃、阶段边界和终态迟到测试。
3. 连接副一次性令牌、设备租约及原子换装由 ledger 负责，覆盖并发占用、过期回收、取消释放和事务回滚测试。
4. 拧紧、扭矩复验、设备故障回执与抽检不可覆盖链由 records 负责，覆盖读数边界、故障重试、幂等冲突和抽检矛盾测试。
5. 双人复核、原因排序及签收/隔离/取消唯一竞争由 arbiter 负责，覆盖人员重合、摘要冲突和终局竞争测试。
6. HTTP 层、操作页面、请求限制、统一错误 envelope、SQLite WAL 提交和重启恢复由 api/store/cmd 支撑，覆盖接口契约、恢复和确定性重复运行测试。

## 独特性

TorqueChain 的核心是钢结构节点内高强螺栓施工顺序前缀、连接副一次性身份令牌、带校准版本的扭矩设备租约、整数扭矩与角度证据以及不可覆盖抽检结果链的组合。它把现场施工记录、实验室复验、资源占用和双人质量签收放在同一可恢复事务边界内，形成面向钢构件连接质量的独特闭环。
